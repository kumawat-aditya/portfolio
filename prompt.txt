/frontend-design 
You are acting as a senior systems engineer, UX designer, and product-level frontend developer.

Your task is to DIRECTLY IMPROVE an existing portfolio website — not redesign from scratch, not replan, but evolve it with precision.

---

## 🧠 CONTEXT

This is NOT a typical “hire me” portfolio.

This is a **personal systems engineering website** designed to:

1. Hook attention (visual + emotional)
2. Create curiosity
3. Show personality and thinking
4. Provide deep technical exploration for those who want it

The current site already does:

* Strong visual design
* Unique tone
* Good interaction foundation

BUT it has a critical flaw:

👉 **Proof exists, but is invisible**

This creates a mismatch:

* Visuals signal: senior / high-level engineer
* Evidence signals: unclear / mid-level

Your job is to FIX THIS GAP — without destroying the original feel.

---

## ⚠️ IMPORTANT RULES (STRICT)

DO NOT:

* Rewrite everything in corporate / resume tone
* Remove personality or human feel
* Turn this into a generic “systems engineer portfolio”
* Invent any data, metrics, or claims
* Add fake production claims
* Replace narrative with dry technical documentation

DO:

* Preserve the tone: human, sharp, thoughtful, slightly raw
* Add proof WITHOUT killing storytelling
* Use ONLY provided data sources
* Improve clarity, trust, and depth
* Keep the experience layered (hook → interest → depth)

---

## 🌐 LIVE SITE (MANDATORY REFERENCE)

You MUST continuously evaluate and improve against:

http://localhost:3000/portfolio/

Do NOT assume structure — VERIFY by checking the running site.

---

## 📂 AVAILABLE DATA (SOURCE OF TRUTH)

You are given real engineering artifacts:

root/raw_data/

Each project contains:

* ARCHITECTURE.md → system design details
* README.md → structured explanation
* logs.txt → real execution logs (critical for proof)
* dashboard.png → real system UI proof
* demo.mp4 → system behavior
* screenshots → state / signals / flows

Also:

* content.json → structured raw truth of all projects

👉 This is your ONLY source of truth
👉 If something is not in these files or missing then, DO NOT fabricate it

---

## 🔥 CORE PROBLEM TO SOLVE

Current issue:

* Claims like “production”, “deterministic”, “zero duplicate execution”
* BUT no visible mechanism or proof attached

Result:

* Feels polished but unverified
* Reduces trust

👉 Your goal:
Make every strong claim feel **earned and grounded**

---

## 🧩 REQUIRED IMPROVEMENTS

### 1. PROOF INTEGRATION (HIGHEST PRIORITY)

Create a reusable concept:

### “Proof Capsule”

Structure:
[Claim]
→ [Visual / Log / Artifact Proof]
→ [Short grounded explanation]

Use:

* logs.txt → real execution snippets
* dashboard.png → system proof
* demo.mp4 → behavior
* ARCHITECTURE.md → system reasoning

Apply this to:

* Home page (above the fold)
* System cards (light version)
* System detail pages (deep version)

---

### 2. HOME PAGE FIX (CRITICAL)

Problems:

* Strong visuals but weak proof
* “Building in production” is unverified
* Feels like startup landing flow

Fix:

ADD (do not remove current vibe):

* One **hard proof strip above the fold**
  Examples:

  * last run stats
  * rows processed
  * execution guarantees (ONLY if supported by logs)

* One **visual proof element**
  (dashboard / logs preview / system state)

* Reduce CTA noise (focus on credibility first)

Goal:
Within 5 seconds user should know:

* what you build
* what actually runs
* why it’s real

---

### 3. SYSTEMS PAGE RESTRUCTURE

Problems:

* All projects have equal weight
* Too much stack-based scanning
* Not enough system pressure visibility

Fix:

* Create hierarchy:

  * Flagship systems (top)
  * Secondary systems
  * Earlier work (separate section)

* Each system should show:

  * problem
  * constraint
  * failure
  * outcome

NOT just tech stack

---

### 4. SYSTEM DETAIL (MAJOR UPGRADE)

Currently:

* Good structure
* Too text-heavy
* Lacks artifacts

ADD:

For each flagship project:

1. Architecture section (visual or structured)
2. Constraint table:

   * latency
   * state control
   * failure boundary
   * recovery behavior
3. “What broke” (real incident using logs)
4. “Why this architecture” (decision reasoning)
5. Proof section (logs / screenshots / demo)

Goal:
Turn this into a **system dossier**, not a blog post

---

### 5. LAB UPGRADE

Problem:

* Feels slightly curated, not raw enough

Fix:

* Add at least ONE real incident post:

  * timeline
  * failure chain
  * fix
  * outcome

* Remove performative elements (like artificial difficulty tags)

* Connect lab posts to actual systems

---

### 6. ABOUT PAGE FIX

Problem:

* Generic philosophy
* Weak differentiation

Fix:

* Remove generic statements
* Add:

“Patterns I default to now”

Derived ONLY from real work:

* state handling
* idempotency
* system boundaries
* failure-first thinking

---

### 7. CONTACT PAGE

Keep tone human, not corporate

---

### 8. 2K / 4K RESPONSIVENESS (IMPORTANT)

Problems:

* Layout stuck in ~1280px thinking
* Too much empty space
* Small typography

Fix:

* Expand container widths (page-specific)
* Add larger breakpoints (2xl, ultra-wide)
* Scale body text using clamp()
* Increase micro-text sizes (nav, tags, labels)
* Make hero + terminal use more horizontal space
* Reduce over-centered layouts

Goal:
Feel intentional on large screens, not stretched

---

## 🎯 EXECUTION STRATEGY

You are NOT writing theory.

You must:

1. Inspect current implementation
2. Modify actual structure/content decisions
3. Use raw_data + content.json to inject real proof
4. Maintain design direction
5. Improve iteratively

---

## 🚫 FAILURE CONDITIONS

Your output will be rejected if:

* It feels like a generic portfolio rewrite
* It removes personality
* It invents metrics or claims
* It ignores provided proof data
* It over-engineers UI without improving trust

---

## ✅ SUCCESS CRITERIA

* Site still feels like “me”, not “resume”
* Stronger trust without losing emotion
* Proof is visible but not intrusive
* One flagship system feels undeniable
* SystemDetail pages feel deep and real
* Large screens feel premium, not empty

---

## 🧠 FINAL INSTRUCTION

This is a **refinement and elevation task**, not a rewrite.

Respect what exists.
Strengthen what’s missing.
Ground everything in reality.

Proceed step-by-step and improve the system.
